@model MVCTraining.Models.ManageUsersViewModel
@{
    ViewBag.Title = "Edit user";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Roles</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)
        <div class="form-group">
            <div class="form-group">
                @Html.LabelFor(m => m.Name, new { @class = "col-md-2 control-label" })
                <div class="col-md-10">
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
                <div class="col-md-10">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                @foreach (var role in Model.Roles)
                {
                    @Html.LabelFor(m => role.RoleId, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => role.RoleId, new { @class = "form-control" })
                    </div>
                }
            </div>
            <div class="form-group">
                @foreach (var claim in Model.Claims)
                {
                    @Html.LabelFor(m => claim.ClaimType, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => claim.ClaimType, new { @class = "form-control" })
                    </div>
                    @Html.LabelFor(m => claim.ClaimValue, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => claim.ClaimValue, new { @class = "form-control" })
                    </div>
                }
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index", "ManageUsers")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}



@*@Html.LabelFor(model => model.Name, new { @class = "control-label" })
    <div>
        @Html.EditorFor(model => model.Name)
        @Html.ValidationMessageFor(model => model.Name)
    </div>

    @Html.LabelFor(model => model.Email, new { @class = "control-label" })
    <div>
        @Html.EditorFor(model => model.Email)
        @Html.ValidationMessageFor(model => model.Email)
    </div>

    @Html.LabelFor(model => model.Roles, new { @class = "control-label" })
    <div>
        @foreach (var role in Model.Roles)
        {
            @Html.EditorFor(model => role.RoleId)
            @Html.ValidationMessageFor(model => role.RoleId)
        }
    </div>

    @Html.LabelFor(model => model.Claims, new { @class = "control-label" })
    <div>
        @foreach (var claim in Model.Claims)
        {
            @Html.EditorFor(model => claim.ClaimType)
            @Html.ValidationMessageFor(model => claim.ClaimType)

            @Html.EditorFor(model => claim.ClaimValue)
            @Html.ValidationMessageFor(model => claim.ClaimValue)
        }
    </div>*@